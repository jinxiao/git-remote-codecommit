[tox]
envlist = py310,py311,py312,py313,py314

skipsdist = True

[testenv]
deps =
    pytest
    flake8
commands =
    pip install -e .
    pip install -r requirements.txt
    pytest

whitelist_externals =
    git
